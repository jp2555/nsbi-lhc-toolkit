# Notes & preferences

- **Workspace location:** `teaching/` inside the NSBI-workflow-tutorial repo (kept in a contained
  subdir to avoid cluttering the git root; untracked). Future `/teach` sessions: run from here, or
  point me at this folder.
- **Audience level (REVISED 2026-06-19):** strong on the ML/physics side, but does NOT assume
  fluency in HistFactory / profile-likelihood *statistics* vocabulary. Lesson 1 pitched too high
  (too much stats jargon at once). Going forward: introduce each stats term in plain words with
  intuition + a concrete example BEFORE using it; minimize symbols; keep cognitive load low for
  knowledge lessons. See Lesson 2 (jargon decoder) as the new baseline register.
- **Grounding rule:** lessons cite primary sources (papers/slides), not parametric memory. Two
  Feb-2026 SBI Blueprint decks were read directly for Lesson 1.
- **Style:** dense, decision-oriented, tied to the κ_λ HH→bbττ mission; Tufte-ish clean HTML.
- **Open dependency:** PDFs of the two indico decks were fetched into the session tool-results dir
  (not the workspace). If needed again, re-fetch from indico event 1600677.
- **Next-lesson fork (see learning record 0001):** ask whether L2 should be methods-deep (reproduce
  R4 on FAIR Universe) or stats-deep (profiling/coverage of learned ratios).
